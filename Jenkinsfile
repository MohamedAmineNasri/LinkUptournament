pipeline {
    agent any

    stages {
        stage('Clean') {
            steps {
                script {
                    sh 'npm run dev'
                }
            }
        }

      
    }
}
